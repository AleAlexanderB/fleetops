# URL del backend FleetOPS
# En desarrollo: dejar vacío (el proxy de Vite redirige a localhost:8077)
# En producción: poner la IP real del servidor Hyper-V
VITE_API_URL=http://192.168.1.100:8077
# API key — debe coincidir con API_KEY del servidor (dejar vacío si no se configuró)
VITE_API_KEY=
