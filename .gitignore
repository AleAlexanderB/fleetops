# Dependencias
node_modules/
client/node_modules/
server/node_modules/
gps-gateway/node_modules/

# Build del frontend
client/dist/
client/.vite/

# Variables de entorno (NUNCA subir al repo)
.env
.env.local
.env.*.local

# Clave SSH del VPS (NUNCA subir al repo)
server_key

# Credenciales y secretos (NUNCA subir al repo)
SECRETOS.md

# Logs del servidor
server.log
server.err
*.log

# Claude / IDE
.claude/

# Scripts de debug temporales (opcionales - podés borrar estas líneas si los querés versionar)
check_*.ps1
debug_*.ps1
test_*.ps1
run_rematch*.ps1
repair_*.ps1
fix_vinculos.*
kill.ps1
query.ps1
plan.md

# Datos de BD locales (si los hubiera)
data/mysql/

# State runtime del gateway (se regenera al correr)
gps-gateway/geocerca-state.json
gps-gateway/*.bak

# State/data runtime local (divisiones.json se persiste en volume Docker)
/data/

# Build output de Vite (outDir: '../public')
/public/

# Contexto para Claude Projects (no es código)
PROYECTO_CONTEXTO.md

# Prototipos sueltos en raíz
/index.html

# Backups sueltos
docker-compose.yml.bak
.env.bak

# OS
Thumbs.db
.DS_Store

# Output de la generación de PDFs de los docs del ecosistema
docs/ecosistema/pdf/
docs/ecosistema/pdf-build/node_modules/
docs/ecosistema/pdf-build/*.html
docs/ecosistema/pdf-build/package-lock.json
docs/ecosistema/pdf-build/manifest.json
