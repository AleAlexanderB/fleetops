# Override para desarrollo local — expone MySQL al host
services:
  db:
    ports:
      - "3306:3306"
  app:
    environment:
      DATABASE_URL: mysql://fleetops:fleetops_2024@db:3306/fleetops
      GATEWAY_URL: http://integration_gateway:3100
